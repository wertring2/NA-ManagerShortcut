<Window x:Class="NA_ManagerShortcut.Views.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Configure Network Adapter" 
        Height="450" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    
    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        
        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E90FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="10" BorderBrush="#3E3E42" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#2D2D30" CornerRadius="10,10,0,0" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Configure Network Adapter" 
                             Foreground="White" 
                             VerticalAlignment="Center" 
                             Margin="15,0,0,0"
                             FontSize="14" FontWeight="SemiBold"/>
                    <Button Content="✕" Width="40" Height="40" 
                            Background="Transparent" Foreground="White" 
                            BorderThickness="0" Click="CloseButton_Click"
                            HorizontalAlignment="Right"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="{Binding AdapterName}" 
                             Foreground="White" FontSize="16" FontWeight="Bold" 
                             Margin="0,0,0,10"/>
                    
                    <TextBlock Text="{Binding AdapterDescription}" 
                             Foreground="#B0B0B0" FontSize="12" 
                             Margin="0,0,0,20"/>

                    <GroupBox Header="IP Configuration" Foreground="White" BorderBrush="#3E3E42">
                        <StackPanel Margin="10">
                            <RadioButton x:Name="DhcpRadio" 
                                       Content="Obtain IP address automatically (DHCP)" 
                                       Style="{StaticResource ModernRadioButton}"
                                       IsChecked="True"
                                       Checked="DhcpRadio_Checked"/>
                            
                            <RadioButton x:Name="StaticRadio" 
                                       Content="Use the following IP address:" 
                                       Style="{StaticResource ModernRadioButton}"
                                       Checked="StaticRadio_Checked"/>
                            
                            <Grid x:Name="StaticIpPanel" Margin="20,10,0,0" IsEnabled="False">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="IP Address:" 
                                         Foreground="White" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" x:Name="IpAddressBox" 
                                       Style="{StaticResource ModernTextBox}" Margin="0,2"/>
                                
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Subnet Mask:" 
                                         Foreground="White" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" x:Name="SubnetMaskBox" 
                                       Style="{StaticResource ModernTextBox}" Margin="0,2"/>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Default Gateway:" 
                                         Foreground="White" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="2" Grid.Column="1" x:Name="DefaultGatewayBox" 
                                       Style="{StaticResource ModernTextBox}" Margin="0,2"/>
                            </Grid>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="DNS Configuration" Foreground="White" BorderBrush="#3E3E42" Margin="0,15,0,0">
                        <StackPanel Margin="10">
                            <RadioButton x:Name="DhcpDnsRadio" 
                                       Content="Obtain DNS server address automatically" 
                                       Style="{StaticResource ModernRadioButton}"
                                       IsChecked="True"
                                       Checked="DhcpDnsRadio_Checked"/>
                            
                            <RadioButton x:Name="StaticDnsRadio" 
                                       Content="Use the following DNS server addresses:" 
                                       Style="{StaticResource ModernRadioButton}"
                                       Checked="StaticDnsRadio_Checked"/>
                            
                            <Grid x:Name="StaticDnsPanel" Margin="20,10,0,0" IsEnabled="False">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Preferred DNS:" 
                                         Foreground="White" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" x:Name="PreferredDnsBox" 
                                       Style="{StaticResource ModernTextBox}" Margin="0,2"
                                       Text="8.8.8.8"/>
                                
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Alternate DNS:" 
                                         Foreground="White" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" x:Name="AlternateDnsBox" 
                                       Style="{StaticResource ModernTextBox}" Margin="0,2"
                                       Text="8.8.4.4"/>
                            </Grid>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#252526" CornerRadius="0,0,10,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0">
                    <Button Content="Apply" Style="{StaticResource ModernButton}" 
                            Width="80" Click="ApplyButton_Click"/>
                    <Button Content="Cancel" Style="{StaticResource ModernButton}" 
                            Width="80" Margin="10,0,0,0" 
                            Background="#5A5A5A" Click="CancelButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>